{
  "name": "lxe-photography",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy": "wrangler deploy",
    "preview": "wrangler dev",
    "check": "node --check script.js && node --check src/worker.js"
  },
  "devDependencies": {
    "wrangler": "4.114.0"
  }
}
